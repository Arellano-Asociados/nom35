# Fase 4.5 — Remates normativos: Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GR-I fuera de ciclo (eventos ATS, 5.3/5.5/6.5), informe renombrado a 7.7 con incisos b/c, registros 5.8 a/c exportables por el RD, y deuda normativa de AUDITORIA.md vacía salvo dependencias externas.

**Architecture:** un evento ATS crea un `compliance_cycles` propio marcado (`traumatic_event_id`) para reutilizar íntegro el mecanismo de tokens/GR-I/canalización; los registros 5.8 son CSVs generados bajo demanda por el RD con auditoría fail-closed (sin Storage); el renombrado 7.7 es integral en superficies y código, pero NO toca `report_type` histórico. Spec: `docs/superpowers/specs/2026-07-14-fase-4.5-remates-normativos-design.md`.

**Tech Stack:** el existente. Sin dependencias nuevas.

## Global Constraints

- Reglas inviolables CLAUDE.md §3. Los registros 5.8 contienen datos de salud por persona: SOLO RD, auditoría fail-closed (regla 5: un `individual_result_access` por resultado incluido en el 5.8 a), jamás en Storage ni correos.
- `description` de un evento ATS documenta el HECHO, nunca datos de salud; copy lo advierte.
- `compliance_reports.report_type='informe_79'` NO se renombra (evidencia histórica); el numeral 7.9 se conserva solo donde refiere periodicidad.
- Toda tabla nueva: RLS + GRANT + FK compuesta + tests. Eventos nuevos al union `EVENTOS_AUDITORIA`. TDD en lógica pura. Commits atómicos en español; push conforme avances.

### Task 1: Migración de eventos ATS + vista bienal + tests RLS

- Create: `supabase/migrations/20260714100000_eventos_traumaticos.sql`
- Modify: `packages/pruebas-rls/src/{fixtures.sql,aislamiento.test.ts}`
- [ ] `traumatic_events` (append-only, RLS select gestión+RD / insert gestión con `reported_by = auth.uid()`), columna `compliance_cycles.traumatic_event_id` + FK compuesta, recreación de `work_centers_alerta_ciclo` con `where traumatic_event_id is null`.
- [ ] Tests RLS: aislamiento; append-only; `reported_by` ajeno rechazado; un ciclo ATS reciente NO apaga la alerta bienal de un centro sin ciclo ordinario.
- [ ] Commit.

### Task 2: Panel de eventos ATS (registro, selección de expuestos, distribución dirigida)

- Create: `apps/web/src/acciones/eventos.ts` (`accionRegistrarEvento` form-action, `accionDistribuirEvento(companyId, eventoId, empleadoIds[])`), `app/panel/[empresa]/eventos/page.tsx` + `eventos/[evento]/page.tsx`, `components/panel/distribuir-evento.tsx` (cliente: checkboxes + confirmación con conteo).
- Modify: sidebar (+“Eventos ATS”), `ciclos/page.tsx` (filtrar `traumatic_event_id is null`), `lib/auditoria.ts` (+`evento_ats_registrado`, `evento_ats_distribuido`), plantilla de invitación reutilizada.
- [ ] Distribución: crea el ciclo ATS on-demand (nombre "Evento ATS — {fecha}", evaluador = registrador), asigna SOLO GR-I a los seleccionados (valida centro), correo por plantilla vigente; detalle muestra asignados/completados y enlaza a canalizaciones del ciclo ATS.
- [ ] Commit.

### Task 3: Registros 5.8 a/c exportables por el RD (TDD)

- Create: `apps/web/src/lib/csv.ts` (extraer `construirCsv`/`escaparCampoCsv` de expediente.ts, re-export), `apps/web/src/lib/registros-58.ts` + test (armado puro de ambos CSVs), `apps/web/src/acciones/registros.ts` (RD-only, fail-closed: `registro_58a_generado` + un `individual_result_access` POR resultado; `registro_58c_generado`), `components/panel/descargar-registro.tsx` (cliente: recibe acción, descarga Blob).
- Modify: `individual/page.tsx` (botón 5.8 a), `gr1/page.tsx` (botón 5.8 c), auditoría (+2 eventos).
- [ ] Tests: columnas exactas, vigentes-only (5.8 a), origen ciclo/ATS (5.8 c), fórmulas neutralizadas; acción sin flag RD → rechazada.
- [ ] Commit.

### Task 4: Informe 7.7 (renombrado integral + incisos b/c + método + nota 7.6)

- Rename: `informes/informe-79-pdf.tsx` → `informe-77-pdf.tsx` (`Informe77Pdf`), `generarPdfInforme79` → `generarPdfInforme77`, `DatosInforme79` → `DatosInforme77`, `armarDatosInforme79*` → `armarDatosInforme77*`; testid `generar-informe-79` → `generar-informe-77` (+E2E informes.spec).
- Modify: PDF (título "Informe de resultados — numeral 7.7", sección b Objetivo determinista por guías, c Principales actividades = `main_activity`, método 7.4 b–d, nota NOM-030 en conclusiones), entrada del ZIP → `informe-7-7.pdf` (+tests expediente e INDICE), UI/copys, `lib/informe.ts`.
- [ ] `grep -ri "7\.9\|informe.79"` limpio salvo periodicidad y `report_type` interno (comentado).
- [ ] Commit.

### Task 5: Expediente — sección de eventos ATS

- Modify: `informes/expediente.ts` (+`eventosTraumaticos?: {fecha, descripcion, expuestos, completados, requierenValoracion, atendidas}[]` → `eventos-traumaticos.csv`, ausencia declarada), `acciones/informes.ts` (lectura por centro del ciclo), tests.
- [ ] Commit.

### Task 6: E2E + docs + cierre

- Create: `apps/web/e2e/eventos-ats.spec.ts` (evento → distribuir a 1 → GR-I con evento → canalización visible al RD → descarga 5.8 c auditada).
- Modify: `informes.spec.ts` (7.7), manual.md (secciones nuevas + renombrados), AUDITORIA.md (remediación 4.5; deuda dimensión 9 vacía; Guía V documentada como no-obligatoria), CLAUDE.md (fila F4.5; referencias 7.7), versión 0.6.0.
- [ ] Suites completas locales; PR; CI verde; merge; tag `v0.6-norma-completa`.
