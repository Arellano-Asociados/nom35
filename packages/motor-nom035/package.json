{
  "name": "@nom35/motor-nom035",
  "version": "0.0.0",
  "private": true,
  "description": "Motor de cálculo NOM-035-STPS-2018: funciones puras, sin I/O ni dependencias de framework",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.0",
    "vitest": "^3.2.0"
  }
}
